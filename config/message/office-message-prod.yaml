spring:
  data:
    mongodb:
      host: mongo
      port: 27017
      database: emos

emos:
  jwt:
    secret: abc123456
    expire: 5
    #    redis保存数据过期时间
    cache-expire: 10

  rabbitmq:
    host: rabbit
    port: 5672